type IUser = {
  id: string;
  email: string;
  avatarUrl: string;
  firstname: string;
  lastname: string;
  userId: string;
  role: string;
  username: string;
  verifications?: object;
};

export type { IUser };
