import NotificationInterface from "@/components/notification-interface"

export default function Home() {
  return (
    <main className="min-h-screen">
      <NotificationInterface />
    </main>
  )
}